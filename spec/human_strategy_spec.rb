require "rspec"
require "../lib/human_strategy"
require "../lib/hand"
require "../lib/deck"

describe HumanStrategy do

  it "should ask human player to choose a card" do
    #deck=Deck.new
    #deck.shuffle!
    #hand=deck.getQuarter(1)
    #human=HumanStrategy.new(hand,nil)
    #
    # @mirror = Mirror.new
    #@mirror.stub!(:gets) { "phrase\n" }
    #@mirror.should_receive(:puts).with("phrase")
    #@mirror.echo
    #human.get_next_move

  end
end